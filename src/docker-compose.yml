services:
  qdrant:
    image: qdrant/qdrant:v1.16.3
    volumes:
      - qdrant_storage:/qdrant/storage
    expose:
      - "6334"
    ports:
      - "6333:6333"
      - "6334:6334"
    networks:
      - backend

volumes:
  qdrant_storage:

networks:
  backend:
    driver: bridge
